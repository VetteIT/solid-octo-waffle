# 🚀 Vercel Deployment Guide

## Як зробити сайт публічним (без аутентифікації)

### Варіант 1: Через Vercel Dashboard (рекомендовано)

1. **Увійдіть на [Vercel](https://vercel.com)** та авторизуйтесь через GitHub
2. **Імпортуйте проект:**
   - Натисніть "Add New..." → "Project"
   - Оберіть репозиторій `VetteIT/solid-octo-waffle`
   - Vercel автоматично визначить конфігурацію з `vercel.json`

3. **Налаштування проекту:**
   - **Framework Preset:** Vite (визначиться автоматично)
   - **Root Directory:** `./` (залиште як є)
   - **Build Command:** `npm run build` (вже в `vercel.json`)
   - **Output Directory:** `dist` (вже в `vercel.json`)
   - **Install Command:** `npm install` (вже в `vercel.json`)

4. **Environment Variables (якщо потрібні):**
   - Додайте змінні оточення, якщо вони використовуються

5. **Deploy:**
   - Натисніть "Deploy"
   - Дочекайтесь завершення білду

6. **Зробіть сайт публічним:**
   - Після деплою перейдіть в **Settings** → **General**
   - Переконайтесь, що **"Public"** встановлено (за замовчуванням так)
   - Якщо є **Password Protection** або **Deployment Protection**, вимкніть їх

### Варіант 2: Через Vercel CLI

```bash
# Встановіть Vercel CLI (якщо ще не встановлено)
npm i -g vercel

# Увійдіть в Vercel
vercel login

# Деплой проекту
vercel

# Зробіть production деплой
vercel --prod
```

### Перевірка публічного доступу

Після деплою Vercel надасть вам URL типу:
- `https://solid-octo-waffle.vercel.app` (або ваш кастомний домен)

**Переконайтесь, що:**
- ✅ Сайт відкривається без логіну
- ✅ Немає password protection
- ✅ Deployment Protection вимкнено (якщо було увімкнено)

### Налаштування кастомного домену (опціонально)

1. Перейдіть в **Settings** → **Domains**
2. Додайте ваш домен
3. Дотримуйтесь інструкцій для налаштування DNS

---

**Примітка:** За замовчуванням всі проекти на Vercel є публічними. Аутентифікація може бути додана тільки якщо ви вручну налаштували **Password Protection** або **Deployment Protection** в налаштуваннях проекту.

